# Список тестов модуля VirtualMachine

## Остановка вычислений

- [x] Остановка виртуальной машины с нулевым кодом
- [x] Остановка виртуальной машины с ненулевым кодом

## Стековые вычисления (int)

- [x] Push + Print значения со стека: 42 → "42"
- [x] Сложение: 10 + 20 → "30"
- [x] Вычитание: 50 - 30 → "20"
- [x] Умножение: 7 * 6 → "42"
- [x] Деление: 100 / 4 → "25"
- [x] Остаток от деления: 17 % 5 → "2"
- [x] Унарный минус: -42 → "-42"
- [x] Удаление значения с вершины стека (Pop): Push 10, Push 20, Pop → "10"
- [x] Сложное выражение: (10 + 5) * 3 → "45"
- [x] Сложное выражение: (100 / 4) + (2 * 3) * 2 → "37"

## Стековые вычисления (float)

- [x] Сложение: 1.5 + 2.0 → "3.5"
- [x] Вычитание: 10.0 - 3.5 → "6.5"
- [x] Умножение: 3.0 * 2.5 → "7.5"
- [x] Деление: 10.0 / 4.0 → "2.5"
- [x] Остаток от деления: 3.5 % 2.0 → "1.5"
- [x] Унарный минус: -1.5 → "-1.5"
- [x] Равенство (true): 1.5 == 1.5 → "1"
- [x] Равенство (false): 1.5 == 2.5 → "0"
- [x] Неравенство (true): 1.5 != 2.5 → "1"
- [x] Неравенство (false): 1.5 != 1.5 → "0"
- [x] Сложное выражение: (1.5 + 2.5) * 2.0 → "8"

## Стековые вычисления (string)

- [x] Конкатенация: "hello" + " world" → "hello world"
- [x] Цепочка конкатенаций: "a" + "b" + "c" → "abc"
- [x] Равенство (true): "hello" == "hello" → "1"
- [x] Равенство (false): "hello" == "world" → "0"
- [x] Неравенство (true): "hello" != "world" → "1"
- [x] Неравенство (false): "hello" != "hello" → "0"

## Встроенные функции (print)

- [x] Вывод int: 123 → "123"
- [x] Вывод отрицательного int: -456 → "-456"
- [x] Два вывода int подряд: 10, 20 → "1020"
- [x] Вывод float: 3.14 → "3.14"
- [x] Вывод отрицательного float: -2.5 → "-2.5"
- [x] Два вывода float подряд: 1.5, 2.5 → "1.52.5"
- [x] Вывод string: "hello" → "hello"
- [x] Вывод пустой строки: "" → ""
- [x] Два вывода string подряд: "foo", "bar" → "foobar"

## Встроенные функции (read)

- [x] ReadI: одно значение
    - вход: 42 → вывод "42"
- [x] ReadI: два значения подряд
    - вход: 10, 20 → вывод "1020"
- [x] ReadI: значение попадает в ExitCode
    - вход: 777 → exitCode == 777
- [x] ReadF: одно значение
    - вход: 3.5 → вывод "3.5"
- [x] ReadF: два значения подряд
    - вход: 1.5, 2.5 → вывод "1.52.5"
- [x] ReadS: одно значение
    - вход: "hello" → вывод "hello"
- [x] ReadS: два значения подряд
    - вход: "foo", "bar" → вывод "foobar"

## Встроенные функции (length)

- [x] length("hello") → "5"
- [x] length("") → "0"
- [x] length("abc") → "3"

## Встроенные функции (substr)

- [x] substr("hello", 1, 3) → "ell"
- [x] substr("hello", 0, 5) → "hello"
- [x] substr("hello", 2, 0) → ""
- [x] substr("Hello, 🚀", 7, 1) → "🚀"

## Сравнение (int)

- [x] Равенство (true): 5 == 5 → "1"
- [x] Равенство (false): 5 == 6 → "0"
- [x] Неравенство (true): 5 != 6 → "1"
- [x] Неравенство (false): 5 != 5 → "0"
- [x] Меньше (true): 3 < 5 → "1"
- [x] Меньше (false): 5 < 3 → "0"
- [x] Меньше (равно): 5 < 5 → "0"
- [x] Меньше или равно (true): 3 <= 5 → "1"
- [x] Меньше или равно (равно): 5 <= 5 → "1"
- [x] Меньше или равно (false): 6 <= 5 → "0"

## Сравнение (float)

- [x] Меньше (true): 1.5 < 2.5 → "1"
- [x] Меньше (false): 2.5 < 1.5 → "0"
- [x] Меньше или равно (true): 1.5 <= 2.5 → "1"
- [x] Меньше или равно (равно): 2.5 <= 2.5 → "1"
- [x] Меньше или равно (false): 3.0 <= 2.5 → "0"

## Переменные

- [x] DefineVar + LoadVar: объявление и чтение переменной
    - x = 42 → вывод "42"
- [x] StoreVar: присваивание нового значения
    - x = 10, затем x = 20 → вывод "20"
- [x] Несколько переменных: a = 3, b = 4 → a + b = "7"
- [x] DefineVar строки: s = "hello" → вывод "hello"
- [x] DefineVar float: f = 3.14 → вывод "3.14"
- [x] PushVars/PopVars: вложенная область видимости
    - outer = 10, inner = 20 → "20", "10", "10"
- [x] PushVars/PopVars: присваивание во внешней области из вложенной
    - outer x = 1, вложенная область меняет x на 99 → вывод "99"

## Логические операции

- [x] And: 1 && 1 → "1"
- [x] And: 1 && 0 → "0"
- [x] And: 0 && 1 → "0"
- [x] And: 0 && 0 → "0"
- [x] Or: 1 || 0 → "1"
- [x] Or: 0 || 1 → "1"
- [x] Or: 1 || 1 → "1"
- [x] Or: 0 || 0 → "0"
- [x] Not: !0 → "1"
- [x] Not: !1 → "0"
- [x] Not: !42 → "0" (любое ненулевое значение)
- [x] Not: !!1 → "1" (двойное отрицание)

## Переходы

- [x] Jump: безусловный переход через инструкции
    - Push 42, Jump 3 (пропуск Push 99), Print → "42"
- [x] JumpIfTrue: переход при ненулевом условии
    - Push 1 (true), JumpIfTrue 4 (пропуск Push 0 и Jump), Push 42, Print → "42"
- [x] JumpIfTrue: нет перехода при нулевом условии
    - Push 0 (false), JumpIfTrue 4 (не срабатывает), Push 99, Print → "99"
- [x] JumpIfFalse: переход при нулевом условии
    - Push 0 (false), JumpIfFalse 4 (пропуск Push 0 и Jump), Push 42, Print → "42"
- [x] JumpIfFalse: нет перехода при ненулевом условии
    - Push 1 (true), JumpIfFalse 4 (не срабатывает), Push 99, Print → "99"
- [x] Реализация if/else через JumpIfFalse и Jump
    - if (1 < 5) { print 1 } else { print 0 } → "1"

## Вызов пользовательских функций (Call/Return)

- [x] Call прыгает на функцию, Return возвращает управление вызывающей стороне со значением
    - func constant(): int { return 42; } → "42"
- [x] Call с одним аргументом: параметр определяется через DefineVar, значение возвращается
    - func double(n): n + n → double(5) → "10"
- [x] Call с двумя аргументами определяет параметры в правильном порядке (стек LIFO)
    - func add(a, b): a + b → add(3, 4) → "7"
- [x] Return восстанавливает переменные вызывающей стороны
    - переменная outer = 99 доступна после возврата из функции → "99"
- [x] Функция может быть вызвана несколько раз
    - inc(1), inc(2), inc(3) → "234"
- [x] Вложенные вызовы: outer вызывает inner, стек возвратов восстанавливается корректно
    - outer() { return inner() + 10; } inner() { return 5; } → "15"

## Таблица переменных: GetAncestor

- [x] GetAncestor(depth) возвращает саму таблицу при совпадении глубины
- [x] GetAncestor(1) из дочерней таблицы возвращает корень (проверяется через переменную)
- [x] GetAncestor(1) из внучатой таблицы возвращает корень
- [x] GetAncestor(2) из внучатой таблицы возвращает промежуточную таблицу
- [x] GetAncestor(3) из внучатой таблицы возвращает её саму
- [x] GetAncestor(0) бросает InvalidOperationException
- [x] GetAncestor(-1) бросает InvalidOperationException
- [x] GetAncestor(depth > длины цепочки) бросает InvalidOperationException
- [x] GetAncestor корректен на каждой глубине (параметризованный тест: depth 1, 2, 3)

## Пользовательские функции — дополнительные сценарии

- [x] Void-функция: Return с Void-значением, Pop после Call — exitCode == 0
- [x] Возвращаемое значение используется в сложении: one() + 41 → "42"
- [x] Функция с условным переходом: abs(-5) → "5" (возврат из разных веток if/else)
- [x] Локальная переменная функции не затирает переменную вызывающего: x=10 в caller, x=99 в func → caller видит "10"
- [x] Трёхуровневые вложенные вызовы: a() → b() → c() → c=1, b=3, a=7 → "7"